{
  "name": "pharos-tx-decoder",
  "version": "0.1.0",
  "description": "An Agent Skill that decodes any Pharos transaction into a plain-English summary — function call, arguments, events, gas, status",
  "type": "module",
  "main": "scripts/decode_tx.js",
  "scripts": {
    "decode": "node scripts/decode_tx.js"
  },
  "keywords": ["pharos", "agent-skill", "transaction", "decoder", "abi", "events", "web3"],
  "license": "MIT",
  "dependencies": {
    "viem": "^2.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
